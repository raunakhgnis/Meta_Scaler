[project]
name = "support-ops-env"
version = "0.1.0"
description = "Customer Support Simulation Environment"
authors = [{ name = "Raunak" }]
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "python-dotenv"
]